.venv/
__pycache__/
*.pyc
.pytest_cache/
out/
.ruff_cache/
.claude/
# Trade settings panel's local override (may hold a real POESESSID session
# cookie) -- see pricing.config.DEFAULT_SETTINGS_PATH and
# docs/data_provenance.md. Must never be committed.
data/local/
# make start/stop's PID files and dev-server logs (scripts/dev.ps1).
.make-pids/
.make-logs/
